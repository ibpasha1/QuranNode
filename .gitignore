# Build output
build/
quran-sim
quran-dump
quran-loop-test
tools/.cache/

# Wi-Fi credentials — NEVER commit (see firmware/secrets.ini.example)
firmware/secrets.ini

# Firmware build output + generated embedded assets (regen: tools/gen_embedded.py)
firmware/.pio/
firmware/sdkconfig.devkit_st7796s
firmware/components/qn/drivers/emb_*.c
firmware/components/qn/drivers/embedded_assets.h

# Tooling venv + fonts (fonts are downloaded, not vendored)
tools/.venv/

# Generated preview/verification images
assets/preview/*.ppm
assets/preview/*.png

# Bundled sample data / audio (large; regenerated by tools/)
sdcard/audio/

# OS cruft
.DS_Store
